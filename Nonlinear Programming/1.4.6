Question 1.4.6:

Function: f(x) = ||x||^2

Example: 

          f(x^{k+1}) = f(x^k - alpha^k(delf(x)/del[delf(x)]))

                     = f(x^k - alpha^k(delf(x)/del[delf(x)]))
   
                     = f(x^k - alpha^k(3||x||x / [3||x||*2*(2||x|| - xx')]))

                     = f(x^k - alpha^k(x^k-1/2*x^k))
                
                     = f((1-alpha^k/2)x^k)

Range of Steps: 0 < alpha^k < 2

Convergence with Unit Stepsize: lim(k->inf) f(x^{k+1}) = 0, when 0 < alpha^k < 2

Rules: Newton's Method: x^{k+1} = x^k - delf(x)/del[delf(x)]
