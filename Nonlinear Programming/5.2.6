Question 5.2.6:

Function: -x^4

Constraint: x=0

The unique global minimum with the function, f(x)=-x^4 prevails an answer, x* = 0.

However, the augmented Lagrangian has no global minimum:

          L(x,lambda) = f(x) + lambda*h(x) + (c/2)*h(x)*h(x)

                      = -x^4 + lambda*x    + (c/2)*x*x

      dL(x,lambda)/dx = -4x^3 + lambda + c*x

                      = 0

                   x* = ??? leads into a cubic formula by a*x^3 + b*x^2 + c*x + d = 0
 
                                                         -4*x^3 +   0   + c*x + lambda = 0
 
                                                        x*: x1 = (1/2)(-c/((3)^{1/3}*((3*(26*lambda^2-c*c*c))^{1/2}-9lambda)^{1/3}) - ((3)^{1/3}*((3*(26*lambda^2-c*c*c))^{1/2}-9lambda)^{1/3})/(3)^{2/3})

                                                            x2 = imaginary answer

                                                            x3 = imaginary answer

Not every c and lambda is a real number, nor a solution to the original unique global minimum, x* = 0;

An alternative method involves an additional term:

           L(x,lambda) = f(x) + lambda*h(x) + (c/2)*|h(x)*h(x)| + |h(x)|^p

                       = -x^4 + lambda*x    + (c/2)*|x*x| + |x|^p

       dL(x,lambda)/dx = -4x^3 + lambda + c*x + p*x*|x|^{p-1}

                       = 0

                    x* = Real values with every lambda and c>0

Rules: Cubic formula: 

        a*x^3 + b*x^2 + c*x + d = 0

        x = (((-b*b*b/(27*a*a*a)+(b*c/(6*a*a))-(d/(2a))) + (((-b*b*b/(27*a*a*a))+(b*c/(6*a*a))^2 +((c/(3*a))-(b*b/(9*a*a)))^1/2)^{1/3} + (((-b*b*b/(27*a*a*a)+(b*c/(6*a*a))-(d/(2a)))-((-b*b*b/(27*a*a*a)+(b*c/(6*a*a))-(d/(2a)))^2-((c/(3*a))-(b*b/(9*a*a)))^3)^1/2)^{1/3} -b/3a
 
Note: Cubic formula derived from year 1545.

