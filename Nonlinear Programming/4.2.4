Question 4.2.4:

Function: f(x)

Constraint: h_i(x)=0

a) L(x,lambda*)=f(x)-lambda**h(x), lambda*>0, L(x,lambda*)<L(x,lambda)

b) L(x,lambda*)=f(x)-lambda**h(x), lambda*<0, L(x,lambda*)>L(x,lambda)

Rules: Proposition 4.2.1 (Second Order Sufficiency Condition)
         dL(x*,lambda*)/dx = 0
         dL(x*,lambda*)/dlambda = 0
         ydel(dL(x*,lambda*)/dx)y > 0
         h(x*)*y=0
         Then a scalar exists near the minimum:
           f(x)>=f(x*)+(c/2)||x-x*||^2 where c>0
