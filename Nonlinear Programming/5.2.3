Question 5.2.3:

Function: f(x1,x2) = (1/2)(x1*x1 - x2*x2) - 3*x2

Constraint: h(x2) = x2 = 0

Method of Multipliers:

  Initial Conditions:   lambda^0 = 1;
                               c = 1;
                              x1 = 1;
                              x2 = 0; 
                             
  Lagrangian: L(x1,x2,lambda) = f(x1,x2) + lambda*h(x2) + c/2(x1)^2

                              = (1/2)(x1*x1 - x2*x2) - 3*x2 + lambda*x2 + c/2(x2)^2
  
  Method of multipliers pre-operation finds a solution for x1, and x2 vectors, in addition to an update formula.

  	1) An optimal solution:      dL(x1,x2,lambda)/dx1 = x1

                	                                  = 0
         
   	                             dL(x1,x2,lambda)/dx2 = (c-1)x2 + lambda - 3

        	                                          = 0 

                	                        (x1*,x2*) = (0, (3-lambda)/(c-1))

	  2) A multiplier solution with an update formula:  lambda^{n+1} = lambda^{n} + c^{n}*h(x2)

                	                                                 = lambda^{n} + c^{n}*x2

                       	                                                 = lambda^{n} + c^{k}*[(3-lambda^{n})/(c^{n}-1)]

  Iteration #1: x1^1 =  x1^{0}+[del[L(x1,x2,lambda)]]/del[del[L(x1,x2,lambda)]];

                     =  1 + del[(1/2)(x1*x1 - x2*x2) - 3*x2 + lambda*x2 + c/2(x2)^2] / del[del [ (1/2)(x1*x1 - x2*x2) - 3*x2 + lambda*x2 + c/2(x2)^2  ]]

                     = 1 + {x1^0 + (c-1)*x2^0 + lambda^0 - 3 }/{1 + c - 1}

                     = 1 + {1+0+1-3}/{c}

                     = 0

                x2^1 = x2^{0}+[del[L(x1,x2,lambda)]]/del[del[L(x1,x2,lambda)]];

                     =  0 + del[(1/2)(x1*x1 - x2*x2) - 3*x2 + lambda*x2 + c/2(x2)^2] / del[del [ (1/2)(x1*x1 - x2*x2) - 3*x2 + lambda*x2 + c/2(x2)^2  ]]

                     = 0 + {x1^0 + (c-1)*x2^0 + lambda^0 - 3 }/{1 + c - 1}

                     = 0 + {1+0+1-3}/{c}

                     = -1

   In one-step, x1 converged to the minimum, x1*=0. While x2* surpassed the constraint, also a flag in program settings. 

   Also, an example interation for Multiplier Method showed x2's functional-minimum at (3-lamba)/(c-1), along with a penalty variable in the denominator, c. The variable, c definitely brings iteration towards a minimum at sufficiently large quantities.



  Iteration
 

  



        

