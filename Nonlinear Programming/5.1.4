Question 5.1.4:

Equation 5.5: q(x,eta) = Xz/eta-e where X = data
                                        eta = precision
                                        z = c-A*lambda = Lagrangian constraint
                                        e = identity matrix

When d = ||q(x,eta)||, then many things happen: 

1) ||(x-x(eta))/X|| <= d/(1-d)

2) F(x)-F(x(eta)) <= d^2/(1-d^2)

3) |c'*x-c'*x(eta)| <= d(1+d)*eta*sqrt(n)/(1-d)

Three plots showed by example the above relationships. 
