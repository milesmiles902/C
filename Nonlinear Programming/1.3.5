Question 1.3.5 (Steepest Descent with Errors)

Function: x^{k+1} = x^k - alpha*(delf(x^k)+e^k)

          f(x) = (1/2)*(x-x*)'*Q*(x-x*)

If q=max{|1-alpha*m|, |1-alpha*M|} where m=smallest eigenvalue and M =largest eigenvalue

Then, x^{k+1} - x^k = -alpha*(delf(x^k)+e^k)

      ||x^{k+1} - x^k || <= ||alpha*(delf(x^k)+e^k)||
 
                         <= ||alpha*delf(x^k)+alpha*e^k||

                         <= alpha*||x-x*||*Q+alpha*||e^k||
 
                         <= alpha*||x-x*||*Q+alpha*delta
           
          ||x^k - x*  || <= ||x^k-x*||*Q + alpha*delta + Q^k*||x^0-x*||-Q^k'*||x^0-x*||*Q^k
 
                         <= alpha*delta/(1-Q) + Q^k*||x^0-x*||

||x^k-x*|| <= alpha*delta/(1-q)+q^k*||x^0-x*||


