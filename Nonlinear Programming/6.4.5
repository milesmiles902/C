Question 6.5.5:

Function: f(x) = max_y{x*y}

Constraint: ||x||<=1

Projection Rule: ||Y||^2 = ||x||^2 + ||Y-x||^2

The solution for the projection rule requires Y = x. While an answer for the Lagrangian, L(x,lambda) = f(x) + lambda*g(x) has minimal solutions (x*,lambda*) = (0,x) or (2,x).

