Question 3.4.1:

Function: f(x)

Constraints: a<=x<=beta

       1) f(x)
       2) x^{k+1} = x^k + alpha^k*sum_a_beta[b-Q*psi]
       3) psi = psi_{i-1} + alpha^k*(b-Q*psi_{i-1})
       4a) ||x^k-x(alpha)|| = 0
       4b) alpha^k->0, sum_0_inf[alpha^k]=inf, then x^k->x*

Rules: Proposition 2.4.1:
         f(x) = (1/2)*x'*Q*x-bx
         x^{k+1} = x^k + alpha^k*sum_1_m[b-Q*psi]
         psi = psi_{i-1} + alpha^k*(b-Q*psi_{i-1})
         a) ||x^k-x(alpha)|| = 0
         b) alpha^k->0, sum_0_inf[alpha^k]=inf, then x^k->x*




