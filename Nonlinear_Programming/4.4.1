Question 4.4.1:

Q = Total product units

n = Market outlet number

d_i = Individual demand at outlet i for product units

p_i = Individiual product price at outlet i

What quantity (s) maximizes the revenue? i.e. Each store sells what quantity?

  Total revenue = sum_i_n [p_i*s_i]

  Constraint: sum_i_n [d_i] >= Q
                          
                          d > 0
  
                          p > 0

  Lagrange equation: L(Q, d_i, p_i, s_i, mu, lambda) = mu*sum_i_n [p_i*s_i] + lambda(Q-sum_i_n[d_i])

  Dual Lagrangian: q(p,s,lambda,x) = mu*sum_i_n [p_i*s_i] + lambda*x

  A solution derivated: 

             dL(Q, d, p, s, lambda, mu, eta)/dp = mu*s

                                                = 0

             dL(Q, d, p, s, lambda, mu, eta)/ds = mu*p

                                                = 0

             dL(Q, d, p, s, lambda, mu, eta)/dlambda = x

                                                     = 0

             dL(Q, d, p, s, lambda, mu, eta)/dx = lambda 

                                                 = 0

   The item number per store (s) equals the sale price (p).

   When without the Dual-Lagrangian, the item amount (s) at each store is zero units with no care in management for store demand.  

What is the cutoff price for each store's demand?

  Total revenue = sum_i_n [p_i*s_i]

  Constraint: sum_i_n [d_i] >= Q
                          
                          d > 0

                          p > 0

  Lagrange equation: L(Q, d_i, p_i, s_i, lambda, mu, eta) = sum_i_n [p_i*s_i] + lambda(Q-sum_i_n[d_i]) + eta*(p-y)

  A solution derivated:

  ...

