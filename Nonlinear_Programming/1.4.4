Question 1.4.4 (Armijo's Rule Along a Curved Path)

Function: x^{k+1} = x^k + alpha^k*[(alpha^k-1)*D*delf(x^k) - alpha^k*delf(x)/del[delf(x)])]

                  where alpha^k = beta^{mk}

Constraints: f(x^k) - f(x^k+beta^m((beta^m-1)*D*delf(x^k) - beta^m*delf(x)/del[delf(x)])]) >= -signma*beta^m*delf(x^k)'[(beta^m-1)*D*delf(x^k) - beta^k*delf(x)/del[delf(x)])]

             c1*min{delf(x^k)*D*delf(x^k), ||delf(x^k||^3} <= -delf(x^k)'*((beta^m-1)*D*delf(x^k) - beta^k*delf(x)/del[delf(x)])]

             ||(beta^m-1)*D*delf(x^k) - beta^k*delf(x)/del[delf(x)])] <= c2*max{||D*delf(x^k), ||delf(x^k)||^{1/2}||}

             where 0 < beta < 1; 0 < sigma < 1/2; c1 < 1; c2 > 1
 
The above constraints show Armijo's Rule, along with Proposition 1.2.3, with a substitution alpha^k = beta^{m*k}

In the limit of beta^k and at  m iterations, alpha^k is a proper substitute for the constraints.

Lastly, the function converges to negative infinity or a finite value. Diminishing stepsizes have stationary points. 

While both Proposition 1.2.1 and 1.3.2 argue similar outcomes, but piecewise to Proposition 1.2.3 in the book's end pages.

Rules: Newton's Method:
        x^{k+1} = x^k - delf(x)/del[delf(x)]

       Armijo Rule (Successive reduction rule)
        f(x^k)-f(x^k+(b^m)*s*(d^k))>=-sigma*(b^m)*s*delf(x*)'*(d^k)
        Reduction factor: b
        Step sizes: (b^m)*s
        Scaling factor: (d^k)
        Trials: m
        Change of the Quadratic Interpolation: delf(x*)'*(d^k)

       Prop 1.2.3: Diminishing Stepsize
        x^{k+1} = x^k + alpha^k*d^k
        c1||delf(x^k)||^2 <= -delf(x^k)'*d^k
        ||d^k||^2 <= c1||delf(x^k)||^2
        If alpha^k->0, sum_0_inf [alpha^k] = inf
        then, f(x^k)->-inf or f(x^k) converges to a finite value and delf(x^k)->0
        Also, x^k is a stationary point of f

