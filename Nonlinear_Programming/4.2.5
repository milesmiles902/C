Question 4.2.5 (Archimedes Problem):

Spherical Segment Area = 2pi*r*h where r is the sphere's radius and h, the segment height

Spherical Segment Volume = pi*h*h(r-h/3)

Lagrange Equation: 

    L(r,h,lambda) = pi*h*h*(r-h/3) + lambda*(1-2pi*r*h)    

Parameters:

    dL(r,h,lambda)/dr = pi*h(h-2lambda)

                      = 0

    dL(r,h,lambda)/dh = -pi(h*h-2h*r+2lambda*r)
 
                      = 0

    dL(r,h,lambda)/dlambda = 1-2pi*h*r

The largest volume spherical segment has the same spherical area. The radius, r is h, also a hemisphere.

 




