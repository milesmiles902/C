Question 5.2.5:

Function: (1/2)*x*Q*x

Constraint: Ax = b

Method of Multipliers: lambda^{n+1} = lambda^n + c^k*(Ax-b)

A convergence check:   lambda^{n+1} = lambda^n + alpha^k(Ax-b)

From 5.2.4, 
  
      lambda^{n+1} - lambda* <= r*|lambda^k-lambda*|

                                where r = |1-c^k/(gamma+c^k)|

                                        = |1-alpha^k/(gamma+c^k)|

A convergence requires a constant r with error:

     r = |1-alpha^k/(gamma+c^k)| <= 1-error

     error <= alpha^k/(gamma+c^k) <= 2-error

In bounds, a convergene is guarantee if: error*(gamma+c^k) <= alpha^k <= (2-error)*(gamma+c^k)

                                                     delta <= alpha^k <= 2c^k - (error*c^k-(error+2)*gamma)

                                                                                ^^^^^^^^^^^^^^^^^^^^^^^^^^^
                                                                                The book removes the term.



                       
