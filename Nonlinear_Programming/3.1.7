Question 3.1.7:

Function: (z-x)'*Q*(z-x)

Constaint: (z-x)'*Q*(z-x) >= 0

The Projection Rule: ||z||^2 = ||x||^2 + ||z-x||^2

Previous notes show a relationship between vectors and Pythagoreas theorem. 

The projection rule becomes Q'[||z||^2) - ||x||^2] = (z-x)'Q(z-x)

